import { combineReducers } from 'redux';

export default const = combineReducers({
  // fill in the modularized reducers here
  // does not need to be a key-value pair object
  /*
    reducer1,
    reducer 2,
  */
});
